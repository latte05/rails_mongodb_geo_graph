<%= form_with(model: site, local: true) do |form| %>
  <% if site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site.errors.count, "error") %> prohibited this site from being saved:</h2>

      <ul>
      <% site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sitename %><br>
    <%= form.text_field :sitename %>
  </div>

  <div class="field">
    <%= form.label :address %><br>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.collection_select:pop_id, Pop.all, :id, :compop_name, {include_blank: true} %>
  </div>




  <div class="field">
    <%= form.label :latitude %><br>
    <%= form.text_field :latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %><br>
    <%= form.text_field :longitude %>
  </div>

  <div class="list">
    <%= form
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
